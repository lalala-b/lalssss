import defaultSettings from '@/settings'

const { tagsView } = defaultSettings

const state = {
  tagsView: tagsView
}

const mutations = {}

const actions = {}

export default {
  namespaced: true,
  state,
  mutations,
  actions
}

